PATH_add cli

activate_path=.venv/bin/activate
if [[ -e $activate_path ]]
then
    source $activate_path
else
  :
fi

# a=$(cs java --jvm adoptium:1.17.0.12 --env)
# echo $a
# eval "$a"

# test -e .cs-java && jsJava=$(cat .cs-java) && eval "$(cs java --jvm  $jsJava --env)"

source_env_if_exists .envrc2
source_env_if_exists .envrc.local
dotenv_if_exists .env
dotenv_if_exists .env.local

unset PS1
